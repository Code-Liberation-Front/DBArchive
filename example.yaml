#
# YAML Scheme (All values are defaults unless specified)
#
# databases: (list of databases to be backed up)
#   database1: (Can be anything)
#     uri: postgres://user:password@localhost:port/database
#     excluded_tables:
#       - table1
#       - ...
#     driver: postgres (databasae type)
#   database2:
#     ...
#args:
#  backup_interval: 0 (hours)
#  backup_count: 0 (number of backups to keep)
#  backup_location: /Backups (path to where backups are stored)
#  tz: America/New_York (set time zone)